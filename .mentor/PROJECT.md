# PROJECT — xd-components

**Type**: Component library (monorepo, per-component npm packages)
**Status**: Modules 1–17 complete (Button family, `tokens`, `image`, `layout`, `unstyled-input`, `input`, `form`, `card`, `Grid` folded into `@asnewyla/layout`, `theme`). `unstyled-checkbox` + `checkbox` (styled) are also done. `Radio` is fully done: `unstyled-radio`, styled `@asnewyla/radio`, and `@asnewyla/radio-group` (a `RadioGroup` wrapper + `RadioGroupContext`, fixing the sibling-desync limitation) are all shipped. `Select / Dropdown` is fully done — `@asnewyla/unstyled-select` (single/multi-select, full keyboard nav, client-side search/filter, a `renderValue` render prop, `invalid`, release-ready) and the styled `@asnewyla/select` (label/error, removable multi-select chips) have both shipped. `Table` is fully done — `@asnewyla/unstyled-table` (plain rendering, single-column sorting, client-side global search, row selection, pagination including first/last-page navigation) and the styled `@asnewyla/table` (thin wrapper — `className` + CSS only, no new props) have both shipped and are live on npm. `Switch` is fully done — `@asnewyla/unstyled-switch` (a native `<input type="checkbox" role="switch">`, controlled/uncontrolled, focus/invalid tracking) and the styled `@asnewyla/switch` (label/error, track/thumb CSS) have both shipped and are live on npm. `Textarea` is fully done — `@asnewyla/unstyled-textarea` (native `<textarea>`, controlled/uncontrolled, no internal value tracking, focus/invalid tracking) and the styled `@asnewyla/textarea` (label/error, `resize: vertical` CSS) have both merged to `main`, **not yet released**. `Tabs` is fully done — `@asnewyla/unstyled-tabs` (compound components, automatic-activation roving-focus keyboard nav, disabled-tab support, focusable active panel) and the styled `@asnewyla/tabs` (underline active-tab indicator, no new props) have both merged to `main`, **not yet released** — changesets staged for all four not-yet-released packages (`.changeset/unstyled-textarea.md`, `.changeset/textarea.md`, `.changeset/unstyled-tabs.md`, `.changeset/tabs.md`), pending the usual `changeset version` + `changeset publish` flow. See Next for the prioritized roadmap of remaining modules.
**Live packages**: All packages built so far except `unstyled-textarea`/`textarea`/`unstyled-tabs`/`tabs` are published on the public npm registry, current versions: `button`/`unstyled-button` `0.2.1`/`0.2.0`, `button-group` `0.1.2`, `icon-button` `0.1.2`, `image` `0.2.0`, `input`/`unstyled-input` `0.2.1`/`0.2.0`, `layout` `0.3.0` (Grid), `form` `0.2.2`, `card` `0.2.1`, `theme` `0.2.0`, `tokens` `0.5.0`, `unstyled-checkbox`/`checkbox` `0.2.0`/`0.2.1`, `unstyled-radio`/`radio` `0.2.0`/`0.2.1`, `radio-group` `0.2.1`, `unstyled-select`/`select` `0.2.0`/`0.2.1`, `unstyled-table`/`table` `0.2.0`/`0.2.0`, `unstyled-switch`/`switch` `0.2.0`/`0.2.0`. Verified via a direct `npm view` check per package post-publish, not just `changeset publish`'s own output — see the `npm view` read-path-lag Pattern below for why that distinction matters.
**Live docs**: https://xavierdelafuente.github.io/xd-components/
**Repo**: https://github.com/xavierDelaFuente/xd-components
**Local**: ~/Repos/2026/xd-components

---

## Stack

| Layer | Choice |
|---|---|
| Language | TypeScript, strict |
| Framework | React 18 |
| Packages | pnpm workspaces |
| Test | Vitest + Testing Library + jsdom |
| Lint/Format | Biome (`biome lint`) — no ESLint/Prettier in this repo |
| Build | tsup (ESM + CJS + d.ts) |
| Styling | Plain CSS (hand-namespaced classes, e.g. `.xd-button`) + `data-*` attributes |
| Docs | Storybook |
| CI | GitHub Actions |
| Deploy | GitHub Pages (Storybook) |

Versions in `package.json` are the source of truth.

---

## Architecture Decisions

| Decision | Choice | Why |
|---|---|---|
| Repo shape | Monorepo | Independently publishable packages, shared tooling |
| Public API | One npm package per component, no barrel | Consumers install only what they use |
| Internal deps | `workspace:*` | Local changes visible without relink |
| Primitive/styled split | Unstyled primitive under any component with real interactive behavior (focus/hover/press/typing) — `unstyled-button`/`button`, `unstyled-input`/`input`. Non-interactive components (`Image`, `Layout`) are one component, no split. | Behavior separable from presentation only where there's behavior to separate; "no split" isn't "no state" (`Image`'s fallback still needs `useState`). |
| State exposure | Render props on primitives with a children slot (`UnstyledButton`); `data-*` attributes on void elements (`UnstyledInput`, no children slot to render into) | Consumer controls rendering from internal state where a slot exists |
| Styling hook | `data-*` attributes, not class-name state | Visible in DevTools, stable for tests |
| CSS scoping | Plain CSS, hand-namespaced classes, not CSS Modules | tsup's CSS Modules support is broken/experimental; revisit only on a build-tool migration |
| CSS distribution | Every styled package exports `./styles.css` as an explicit subpath, `sideEffects: ["*.css"]` | tsup doesn't inject CSS into the JS bundle; without an explicit subpath export the import 404s under strict `exports` resolution, and `sideEffects: false` lets bundlers tree-shake it away entirely |
| CSS token prefix | `--xd-*`, independent of the npm scope | Stable design-language namespace; plenty of real projects diverge here |
| Tokens dependency model | Soft dependency — every component's CSS keeps its own hardcoded fallback (`var(--xd-color-primary, #0d9488)`); no `package.json` declares a dependency on `@asnewyla/tokens` | A hard dependency via `@import` in published CSS is fragile across bundlers; soft dependency degrades to a working default with zero setup |
| Cross-family context ownership | A context consumed by a family of components lives in that family's base package (`ButtonGroupContext` in `@asnewyla/button`; `FormFieldContext` in `@asnewyla/input`), not in the consuming package | Keeps the dependency graph one-directional, avoids circular workspace deps |
| Context-consumption weight | Consume an ancestor context directly when the surface is small (`Button`/`ButtonGroupContext`: 3 keys, one-line `??` fallback); split into a dedicated wrapper component once it's heavier (`FormFieldInput` wraps `Input`: required `name`, six rule props, a registration effect, ref-merging, blur composition) | Match structure to weight, not to shape — a pattern that fits a light case doesn't automatically fit a heavier one |
| `FormFieldInput` package | Ships from `@asnewyla/form`, importing `Input`/`FormFieldContext`/`useFormFieldContext` from `@asnewyla/input` as an external dependency | It's a `Form`-consuming component (registers into `FormFieldContext`, calls `validateField`) more than an `Input`-family one; a form consumer imports `Form` and `FormFieldInput` from one package |
| Input value model | Uncontrolled by default (native `defaultValue`), optional `value`/`onChange` for controlled use | Matches native `<input>`; keeps `Form` from becoming a state manager |
| Native vs. custom validation | `<form noValidate>` — `Form`'s own JS validation is the only validation that runs; the browser's native constraint validation (triggered by the real `required`/`pattern`/etc. attributes `FormFieldInput` sets) is disabled | Without `noValidate`, native validation blocks the `submit` event entirely on an invalid required field, before `handleSubmit` ever runs — `required`/`pattern`/etc. still need to be real HTML attributes (for the "used outside a `Form`, native validation still works" contract), but the browser's own enforcement of them has to be turned off for `Form`'s validation and error summary to be the thing that actually runs |
| `Card` image prop shape | `image?: Omit<ImageProps, 'radius'>`, spread onto an internal `Image` | Full `Image` flexibility (`fit`, `aspectRatio`, `fallback`, native `<img>` attributes) for free, matching the `Omit`-blocklist composition convention already used for `IconButton`→`Button` — a hand-picked minimal subset would need its own follow-up the first time a consumer needed `fit` or `aspectRatio`. `radius` is the one exclusion: `Card`'s own `radius` has to govern the outer shape (including the image's top corners), so a separately configurable image `radius` would just create a way to fight it |
| Form value collection | `new FormData(form).entries()` at submit, per-field at blur — not ref-walking, not a duplicated React state tree | `FormFieldInput` already puts a native `name` on the input, so `FormData` is the correct primitive. Consequence (accepted, tested): an unregistered `<input name>` inside a `Form` still contributes its value to `onSubmit` even without `FormFieldInput` |
| Error display | Both: inline per-field error on `Input` (`aria-describedby`) and a `Form`-level error summary (`role="alert"`, links to each field's `id`, focus moves to the summary on failed submit) | WCAG 3.3.1 requires the error identified at the field; a summary is a complement, not a substitute |
| Form validation scope | `required`, `pattern`, `minLength`/`maxLength`, `min`/`max`, one custom sync validator | Async validation, field arrays, conditional schemas are out of scope for a v0.x component library |
| Versioning | Independent per-package via Changesets (`access: "public"`) | Packages have divergent dependency graphs (`Image`/`Layout` don't depend on `Button`); lockstep would force noisy, misleading bumps on unrelated packages |
| npm scope | `@asnewyla` (personal npm username) | `@xd` was already taken; `@xd-components` was available but not worth reverting the existing `@xd` naming across the codebase |
| Publishing | `pnpm publish`, never plain `npm publish`, for any package with a `workspace:*` dependency; personal-account granular token with "Bypass 2FA" for non-interactive publish | `npm publish` doesn't resolve the `workspace:` protocol; a live OTP prompt has no TTY in a non-interactive shell |
| License | MIT, `LICENSE` file copied into every package directory | npm only includes `LICENSE` in a published tarball when it physically exists inside that package's own directory |
| Storybook | v10.5.10 via official `init`, not `@storybook/addon-essentials` | That addon has been empty since Storybook 9 |
| `Video` controls model | Wrap native `<video controls>` — one component, no primitive/styled split (same category as `Image`/`Card`). Native `controls`/`autoPlay`/`loop`/`muted`/`preload`/`playsInline`/`poster` via `{...rest}` passthrough; no custom play/scrub/volume UI | A custom control bar is a large, separate a11y surface (ARIA slider scrubbing, fullscreen API, keyboard) — deferred behind the "wait for the second consumer" bar. Native `<video>` already carries role, keyboard operability, and its own accessible controls |
| `Video` source shape | `src?: string` only, like `Image` | Covers the common case with the smallest surface; multi-`<source>` format fallback added when a consumer actually needs webm+mp4 |
| `Tooltip` positioning | CSS-only via a `side` prop → `data-side` (`top`/`right`/`bottom`/`left`), default `top`. No collision detection / flip / shift for v0.x | Same "wait for a real need" bar as `Grid`'s alignment and `Card`'s variant; a positioning engine (or `floating-ui`) is a big dependency/complexity jump that a first release doesn't need |
| `Tooltip` trigger model | `TooltipTrigger` takes a single child and `cloneElement`s it — injects `ref`, `aria-describedby` (while open), and the pointer/focus/blur handlers onto whatever the consumer passes | Tooltips attach to arbitrary triggers (icon buttons, links, inputs), not always a `<button>`; matches Radix/Reach. Every `@asnewyla` interactive component already forwards `ref` + props |
| `Tooltip` rendering | `TooltipContent` renders inline (sibling of the trigger), not portalled, and returns `null` when closed | Portal rendering is deferred with the same note `unstyled-select` carries — an `overflow: hidden` ancestor can clip it; revisit alongside `Dialog`'s portal work |
| `Video` controls model | Wrap native `<video controls>` — one component, no primitive/styled split (same category as `Image`/`Card`). Native `controls`/`autoPlay`/`loop`/`muted`/`preload`/`playsInline`/`poster` via `{...rest}` passthrough; no custom play/scrub/volume UI | A custom control bar is a large, separate a11y surface (ARIA slider scrubbing, fullscreen API, keyboard) — deferred behind the "wait for the second consumer" bar, same as `Card`'s missing `variant`. Native `<video>` already carries role, keyboard operability, and its own accessible controls |
| `Video` source shape | `src?: string` only, like `Image` — not a `sources: {src,type}[]` array | Covers the common case with the smallest surface; multi-`<source>` format fallback is added when a consumer actually needs webm+mp4, not preemptively |
| `Dialog` foundation | Native `<dialog>` element + `showModal()`/`close()`, not a custom `<div role="dialog">` + portal | Focus trap, background inertness, top-layer stacking, `::backdrop`, and Escape-to-close all come from the browser — hand-rolling them is ~150 lines of a11y-critical code. Consistent with the repo's native-first lean (`Switch`, `Textarea`, `Video`) |
| `Dialog` test environment | A ~30-line `HTMLDialogElement` shim in the dialog packages' `test-setup.ts` (`show`/`showModal`/`close`/`returnValue`, `close`/`cancel` events, Escape wiring) | jsdom through 30.x still ships an empty `HTMLDialogElement` (only the `open` attribute reflects). The shim covers exactly the wiring the primitive depends on; the focus trap / inertness / `::backdrop` it can't reproduce are browser-native behavior verified in Storybook, the same documented-gap approach as `unstyled-button`'s `:focus-visible` |
| `Dialog` API shape | Compound components (`Dialog`/`DialogTrigger`/`DialogContent`/`DialogTitle`/`DialogDescription`/`DialogClose`), uncontrolled by default with optional `open`/`onOpenChange` — mirrors `Tabs` | Trigger wiring and `aria-labelledby`/`aria-describedby` cross-referencing are automatic; matches the `Tabs` precedent for a compound primitive with a shared context in the base package |
| `Dialog` presets | One centered modal in the styled layer for v0.x, plus a `position` prop (`center`/`left`/`right`/`bottom`) that is a CSS-only `data-position` difference — no separate `Drawer`/`Sheet` component, no `AlertDialog` yet | Same "one primitive, thin variants" precedent as `Layout`→`Stack`/`Group`; `AlertDialog` (`role="alertdialog"`, required labelled actions) waits for a real consumer need |

---

## Constraints

- Every package must be independently installable and buildable.
- No cross-package imports except through published entry points.
- Peer deps on React; never bundle it.
- Any CI job running `test`, `type-check`, or `storybook:build` must run `build` first — `workspace:*` deps resolve through the consumed package's `dist/`, which is gitignored.
- Every component module ships its own Storybook stories as part of that module's scope, not a later catch-up pass.
- Any PR that changes a published package's behavior includes a changeset (`pnpm changeset`) before merge.
- Whenever an implementation's behavior in an edge case is uncertain and gets accepted rather than changed (a native HTML attribute silently blocking an action, a framework default overriding an expectation, etc.), add a dedicated test that locks in that exact behavior — not just a comment explaining it inside another test. The test suite is the contract for what the library actually does, quirks included, not only its intended features.

---

## Packages

**Button family** — `@asnewyla/unstyled-button` (primitive: polymorphic `as`, `forwardRef`, render props `{isHovered,isPressed,isFocused,isFocusVisible,isDisabled}` — `isFocusVisible`/`data-focus-visible` is implemented (`e.target.matches(':focus-visible')`) but deliberately untested: confirmed via a throwaway probe that jsdom's `:focus-visible` matches `true` for both a Tab-focus and a plain `.focus()`/click-triggered focus, so it can't distinguish keyboard from mouse here — a test would either be unwritable or falsely reassuring); `@asnewyla/button` (variants/sizes/icon slots, styled, resolves `variant`/`size`/`disabled` via `prop ?? group?.x ?? default`); `@asnewyla/icon-button` (composes `Button` via `Omit<ButtonProps,...>`, mandatory `aria-label`, no `as`); `@asnewyla/button-group` (`role="group"`, propagates `variant`/`size`/`disabled` through `ButtonGroupContext`, which lives in `@asnewyla/button`).

**`@asnewyla/tokens`** — Color/spacing/radius/typography scales as `--xd-*` custom properties. CSS-only package (`tsup` entry is the bare CSS file, no JS/d.ts). Spacing/radius/typography in `rem`; `radius-full` (`9999px`, a pill trick value) and `border-width`/`focus-ring` (fixed-pixel accents) are the deliberate `px` exceptions. Consumed as a soft dependency everywhere (see Architecture Decisions).

Gained a surface/text tier (`--xd-color-surface`, `--xd-color-surface-hover`, `--xd-color-text`, `--xd-color-text-muted`) and `--xd-color-border-strong`/`--xd-color-focus`/`--xd-shadow-sm`/`--xd-shadow-md` while building `@asnewyla/select` — the first component to own a floating surface (its popover); every prior component only ever needed the page's own background/text, which the consumer's app already owns. `-border` stays decorative (~1.3:1, fine for a hairline separator); `-border-strong` is for an interactive control's own boundary, held to WCAG 1.4.11's 3:1. `tokens.css`'s dark-mode `@media` block also gained a `:not([data-mode="light"])` guard — without it, `data-mode="light"` couldn't force light against the base palette while the OS was dark. Separately, light-mode `--xd-color-primary` moved `#0d9488` → `#0f766e`: white-on-primary measured 3.74:1, below AA for normal-size text — a real accessibility bug repainting the whole library's primary-filled surfaces, confirmed with the user before changing it given the blast radius (every existing component using `--color-primary` as a fill).

The three themes built during that same pass (`theme-paper`/`theme-sand`/`theme-lavender`) were never released and were entirely superseded before shipping — the user replaced them with six new themes with a different naming/creative direction: `theme-terra`, `theme-almanac`, `theme-block`, `theme-graphite`, `theme-rubber`, `theme-terminal`. Each is a complete, standalone set of every `--xd-*` token (paints `background-color`/`color`/`font-family` directly on `[data-theme="name"]` too, so a page mounting only the theme file still renders correctly with no other styling). Structurally different from the retired themes: a theme selector is just `[data-theme="name"]` on its own — no bare `:root:not([data-theme])` combined into it, since `tokens.css`'s own `:root` is now the sole untagged default. `tsup.config.ts`'s `entry` list and `package.json`'s `exports` map both still referenced the old three filenames after the swap (the new themes literally couldn't be built or imported without fixing this) — caught while verifying the build, not something the user had touched.

The same pass added several token tiers beyond the theme swap itself, all consumed by real components now, not just defined: a font-family tier (`--xd-font-family`, `--xd-font-family-mono`, `--xd-font-weight-medium`, `--xd-font-weight-bold`); label voicing (`--xd-letter-spacing-label`, `--xd-text-transform-label`) — read by every `-label` element (`Input`, `Checkbox`, `Radio`, `Select`) and by `Button`'s own visible text, since a button label is a "label" in the same sense; `--xd-space-3xl` and `--xd-border-width-thick`; `--xd-frame-border-width` (what `Card`'s border and `Select`'s popup border read now, instead of the plain hairline, so a theme can thicken frame surfaces independently of every other hairline — Block's theme sets this to a literal `2px` rather than `var(--xd-border-width-thin)`, since its whole aesthetic is thick ink frames); a control border pair `--xd-control-border-width`/`--xd-control-border-color` (0/`currentColor` by default, so inert until a theme opts a filled control into a real outline — `Button`'s `border: none` became this); `--xd-control-height-md` (`Button`'s `md` size, `Select`'s trigger, and `Input`'s field all read this now instead of a hardcoded height); and a control-specific elevation tier separate from surface elevation — `--xd-shadow-control`/`--xd-shadow-control-active`/`--xd-press-transform`, all `none` by default, which is what lets Block give buttons a hard offset shadow that collapses on press and Rubber give them a `scale(0.97)` press, without touching Card/Select's own surface shadows.

`theme-lavender.css` (superseded) had no surviving design draft with values for the surface/text/border-strong/focus/shadow tier — derived by hand instead, following paper/sand's exact method (same lightness values, lavender's own hue/chroma), flagged in the file's own comment as not independently contrast-verified the way paper/sand's headers reported. That file no longer exists, but the same derive-by-hand situation didn't recur for the terra/almanac/block/graphite/rubber/terminal set — the user supplied every token for all six themes directly, nothing needed inferring.

Test coverage for the theme files (`themes.spec.ts`) checks every token per theme, light and dark (both the `prefers-color-scheme` media block and the explicit `[data-theme][data-mode="dark"]` override), plus that typography/spacing/radius/border-width/density/press-transform stay constant across modes (only color and elevation switch) — 142 tests total across `tokens.spec.ts` + `themes.spec.ts`, up from 50.

**`@asnewyla/image`** — Non-interactive, no primitive split. `alt` required at the type level, no default. `fit`/`radius` drive `data-*`-mapped tokens; `aspectRatio` is a raw CSS value via inline `style`. Fallback tracked as a `hasErrored` boolean, not a copy of `src` — reset via a `useEffect` keyed on `src`.

**`@asnewyla/layout`** — `Layout` (`direction`/`gap`/`align`/`justify`/`wrap` as `data-*`-mapped tokens) plus `Stack`/`Group` as thin wrappers that spread props first and force `direction` last, so the fixed axis can't be overridden even by bypassing the type system.

**`@asnewyla/unstyled-input` + `@asnewyla/input`** — Uncontrolled by default. State exposed via `data-focused`/`data-invalid`/`data-disabled`, not render props (void element, no children slot). `InputProps` derives from `UnstyledInputProps`, not raw `InputHTMLAttributes`. `invalid` is derived from `!!error`, not a separate prop. `@asnewyla/input` also exports `FormFieldContext`/`FormFieldProvider`/`useFormFieldContext` (see Cross-family context ownership) — `Input` itself has zero knowledge of `Form`.

**`@asnewyla/form`** — `Form` (renders `<form noValidate>`, owns validation, provides `FormFieldContext`; internals split into `validation.ts` — pure `validateValue`, no React — `useFieldRegistry.ts` — the field `Map`/`errors` state, register/unregister/validate, and `invalidFields()` for the summary — and `FormErrorSummary.tsx`/`.css`, the summary UI itself, split out to match the `validation.ts`/`useFieldRegistry.ts` file-per-concern convention) and `FormFieldInput` (wraps `Input`: `name` + rule props + registration effect + ref-merging + blur-triggered validation; no-ops gracefully with no `FormFieldProvider` ancestor — native attributes like `required`/`pattern` still reach the DOM either way). Field-level markup still comes entirely from `@asnewyla/input`'s stylesheet, but `Form` now also ships its own CSS for the error summary (destructive-colored border/heading/links, `--xd-color-destructive` token) — a consumer needs to import both. See Form value collection, `FormFieldInput` package, and native validation vs. custom validation decisions. `LICENSE`/`README.md`/Storybook stories in place — see Release readiness below for what's still open.

**`@asnewyla/card`** — Shipped, live at `0.2.0` (22/22 tests, build/type-check/lint clean, verified in a real browser via Playwright screenshots). Own package, non-interactive, no primitive split (same category as `Image`/`Layout`). `CardProps = ComponentPropsWithoutRef<'div'> & { children (required), image?: Omit<ImageProps, 'radius'>, padding?: 'sm'|'md'|'lg' (defaults 'md'), radius?: 'sm'|'md'|'lg'|'full' (no default, square corners) }` — spreads native div props (`className` merged with `.xd-card`, not replaced; other rest props like `id`/`aria-label` pass through), matching the `ButtonGroup`/`Layout` composition convention. No `variant`/border-vs-shadow prop for v0.x — a single fixed surface treatment until a second real use case demands more, same "wait for the second consumer" bar Module 10 used for tokens.

`Card.css`: padding and the full-bleed image negation both read from one private `--_padding` custom property per `data-padding` value (`--_bg`/`--_bg-hover`-style private, not a token) — the image negates `--_padding` via a matching negative margin to reach the card's edges, then `overflow: hidden` + the card's own `border-radius` clips it, so no separate image-radius handling is needed at all. Verified via Playwright: image renders full-bleed and correctly clipped to the card's rounded corners; a naive "image width should equal the card's outer `getBoundingClientRect` width" check was off by exactly the border width (2px) — expected, since the image fills to the *inside* edge of the border, not underneath it; confirmed correct visually, not a bug.

Two implementation gaps found and fixed along the way: (1) first pass had `padding` typed required despite a runtime default, a hand-rolled `image` shape instead of `Omit<ImageProps, 'radius'>`, and a `radius` union missing `'full'` — all passed `vitest` (which doesn't type-check) but failed `tsc`/`tsup`'s dts step. (2) `CardProps` didn't accept `style`/`className`/any native div attributes at all, unlike every other non-polymorphic component in this codebase — fixed via the same `ComponentPropsWithoutRef<'div'>` composition every sibling package already uses. Storybook stories (`storybook/Card.stories.tsx`: `Default`, `WithImage`, `PaddingSizes`, `RadiusSizes`) use inline SVG data URIs for the demo image, not a network URL — matches `Image.stories.tsx`'s existing convention and is why the first `WithImage` screenshot came back blank (an external `picsum.photos` URL, no network access in this environment). `LICENSE`/`README.md` added, dry-run tarball verified (11 files), changeset added. Release-ready — just needs merging and shipping through the normal `changeset version` + `changeset publish` flow.

**Grid** — Done. Folds into `@asnewyla/layout` as a fourth export, not a standalone package — matches the `Stack`/`Group` precedent (closely related "arrange these children" primitives sharing one package, same soft dependency on the gap token scale) over a literal reading of "one package per component," which was written to avoid forcing unwanted deps, not to split apart things this closely related. `GridProps`: `columns?: number | string` (a number renders `repeat(N, 1fr)`; a string passes straight through as `grid-template-columns` — a freeform escape hatch via inline `style`, same pattern as `Image`'s `aspectRatio`, not a `data-*` attribute since the value space isn't a finite enum), `gap?: 'sm'|'md'|'lg'` (reuses `Layout`'s existing gap scale/tokens exactly), `align?`/`justify?: 'start'|'center'|'end'` (map to `align-items`/`justify-items`, not `align-content`/`justify-content` — track-distribution alignment deferred until a real need shows up), plus the rest of native `<div>` props via `Omit<HTMLAttributes<HTMLDivElement>, 'className'|'children'>`, matching `Layout`'s own composition exactly (its direct sibling in the same file). `data-testid` defaults to `'grid'`, matching `Layout`/`Stack`/`Group`. `Grid.css` styles `data-gap`/`data-align`/`data-justify` the same way `Layout.css` does; `display: grid` lives in the stylesheet, not inline (only `gridTemplateColumns`, freeform, stays inline). Storybook stories (`storybook/Grid.stories.tsx`) verified in a real browser via Playwright — column-track proportions and align/justify-items centering both confirmed correct; one story (`ThreeColumns`) needed a width-constraining wrapper like its siblings already had, since without one each `1fr` track collapses to its content's minimum width instead of splitting available space.

Two real gaps found and fixed during implementation, worth remembering for the next component built from scratch rather than copied from a sibling: (1) `GridProps` was originally a hand-rolled closed object type instead of extending native `<div>` props — TypeScript specially exempts `data-*`/`aria-*` attribute names from prop-type checking on *any* JSX element regardless of the component's declared props, so a test passing `aria-label` looked like it proved "arbitrary attribute passthrough" while an ordinary prop like `id` actually failed to type-check — a real TypeScript consumer couldn't have written `<Grid id="foo">` at all, even though the runtime `{...rest}` spread would have handled it fine. Fixed by composing from `Omit<HTMLAttributes<HTMLDivElement>, 'className'|'children'>` like `Layout`, and swapped the test's `aria-label` for `id` so it actually proves what it claims — a `data-*`/`aria-*`-based passthrough test is not real evidence of native-prop composition. (2) The `{...rest}` spread came *before* the protected `data-gap`/`data-align`/`data-justify`/`className` values instead of after — exactly the ordering gotcha already documented in Patterns below, just not yet applied to `Grid`'s first draft.

**`@asnewyla/theme`** — Merged, not yet released (see Status). New package, not folded into `@asnewyla/tokens`: `tokens` is deliberately CSS-only (no JS/React entry point at all), and `ThemeProvider` is a real React component, so adding it there would force a React dependency onto every consumer who only wants the default stylesheet. `ThemeProviderProps = { theme?: string; mode?: 'light' | 'dark'; children: React.ReactNode }` — both `theme` and `mode` optional and independent (a consumer can control either axis alone, both, or neither); `theme` stays a plain `string` (the package has no knowledge of which theme names exist — that's defined entirely by whichever `--xd-*` stylesheets a consumer imports), `mode` is a real union since every `@asnewyla/tokens` theme file only ever matches `data-mode="light"` or `"dark"` literally. Two independent `useEffect`s, one per prop, each setting/removing its own attribute on `document.documentElement` (`<html>`) — deliberately not a wrapping `<div>`, since CSS custom property inheritance follows the real DOM tree, and a future portal-based component (`Dialog`, `Select`, `Toast`, mounted into `document.body`) would sit outside `ThemeProvider`'s own JSX position but still under `<html>`. Leaving either prop unset removes the attribute entirely rather than falling back to a default value — a default like `mode="light"` would permanently defeat automatic `prefers-color-scheme` detection for every consumer, since the dark-mode blocks in every theme file are scoped `:not([data-mode="light"])` specifically so the OS preference keeps working until someone explicitly opts in.

`@asnewyla/tokens` gained three complete, standalone theme stylesheets (`theme-paper.css`, `theme-sand.css`, `theme-lavender.css`), each scoped under `[data-theme="x"]` as well as a `:root:not([data-theme])` fallback (not bare `:root`) so several theme files can be imported together without one's fallback silently overriding another's explicitly active theme — see the real bug this fixed, below. Each theme also supports the existing `data-mode="light"|"dark"` explicit override alongside `prefers-color-scheme`, independent of which theme is active. Adapted from external design drafts that used an entirely different, richer token vocabulary (`--xd-primary`, `--xd-surface`, `--xd-shadow`, `--xd-font`, ...) than this package's actual scale-based one (`--xd-color-primary`, `--xd-radius-sm/md/lg`, ...) — only values with a genuine current-token equivalent were mapped over (color, radius scale via consistent offset-derivation from the draft's single value, motion); categories with no current token (surface/text/muted color pairs, box-shadow, font-family, a second border tier) were deliberately dropped rather than invented, documented inline in each file's header. `themes.spec.ts` (25 of the package's 35 tests) locks in each theme's exact values plus two regression guards: balanced `/* */` comment delimiters (see the real bug below) and the `:not([data-theme])`-guarded selector shape.

Two real bugs found during this module, both now covered by regression tests: (1) a stray `*/` inside a CSS comment's own prose (`--xd-surface*/--xd-text`) silently closed the comment early, caught only by luck via a `tsup` build warning — now a permanent comment-balance test across every CSS file in the package. (2) Each theme file's bare `:root` fallback (for the "single theme, no `ThemeProvider`" import case) had the *same* CSS specificity as `[data-theme="x"]`, so with multiple themes imported together (`ThemeProvider`'s actual primary use case), whichever theme file was imported last silently won on every element — including one with an explicitly *different* `data-theme` set — regardless of which theme was actually active. Fixed by scoping the fallback to `:root:not([data-theme])`, which only applies when no theme attribute is present at all. Found via real-browser verification (Playwright, not just `vitest`/jsdom, which can't compute real CSS cascade from linked stylesheets) — the exact kind of bug that only shows up once you actually check the rendered result rather than trusting that each piece looks right in isolation.

**`@asnewyla/unstyled-checkbox`** — Done. The whole `Radio` side of this roadmap item (`unstyled-radio` + `radio` + `RadioGroup`) hasn't started yet. `UnstyledCheckboxProps = { checked?, defaultChecked?, indeterminate?, onChange?, disabled?, invalid? } & Omit<InputHTMLAttributes<HTMLInputElement>, 'type'|'checked'|'defaultChecked'|'onChange'|'disabled'|'invalid'>` — same `data-*`-state-on-a-void-element shape as `UnstyledInput` (not render props; a native `<input>` has no children slot), but combines two patterns that previously lived in separate primitives: `UnstyledInput`'s controlled/uncontrolled duality (`checked`/`defaultChecked`/`onChange`) *and* `UnstyledButton`'s internal-state-for-`data-*`-exposure tracking (`UnstyledInput` never needed this — text content is visually self-evident; `data-checked` needs real tracking even in uncontrolled mode because it's what a styled wrapper hooks into to paint a custom checkmark, since there's no native visual equivalent once the checkbox is customized). `isControlled = checked !== undefined` branches which source of truth `isChecked` reads from; `internalChecked` state (seeded from `defaultChecked`) only updates when *not* controlled, matching the standard controlled/uncontrolled-merge shape. Deliberately does **not** depend on `@asnewyla/unstyled-input` (an early draft did) — reuses its *shape*, not the package itself: this codebase has a standing precedent (`OverridableProps`, the `className`-merge ternary) that small, cheap-to-duplicate logic (here, ~10 lines of focus tracking) stays duplicated per primitive rather than composed away, and `UnstyledInputProps`' text-value vocabulary (`value`/`defaultValue`) has no use here anyway.

`indeterminate` is a DOM-only property with no HTML attribute or plain-JSX-prop reflection — confirmed empirically via a throwaway probe (React accepts it as a JSX prop with no error, but the DOM property silently stays `false` and no attribute is ever set; React logs a "non-boolean attribute" warning). Applied imperatively via an internally-owned ref + `useEffect`, using the same ref-merging shape `FormFieldInput` already established (`setRefs` writes to both the internal ref and whatever `ref` the consumer forwarded, handling both function- and object-ref forms).

`{...rest}` spreads *before* the protected `type`/`checked`/`data-*`/`aria-invalid` values, matching the documented ordering Pattern below — deliberately not the same order `UnstyledInput` itself uses (`{...rest}` last), which was noticed as a latent instance of the same class of bug `Grid` had: a consumer could pass a raw `data-focused`/`data-invalid` attribute and silently override the internally-computed one. Not fixed in `unstyled-input` (already-shipped, out of scope for this module), just avoided here.

**`@asnewyla/checkbox`** — Done. `CheckboxProps = Omit<UnstyledCheckboxProps, 'invalid'> & { label: string; error?: string }` — a straight port of `Input`'s shape onto checkbox semantics (label association via `useId()`-generated or explicit `id`/`htmlFor`, `invalid` derived from `!!error`, error `<div>` linked via `aria-describedby`, `className` merged onto the field element itself rather than the wrapper, `forwardRef`, `{...rest}` passthrough) — no new architectural decisions beyond what `Input` already established. `Checkbox.css` uses `appearance: none` plus a `::after`-drawn checkmark/dash, painted from `[data-checked]`/`[data-indeterminate]` (not `:checked`/`:indeterminate` pseudo-classes — same DevTools-visible, test-stable reasoning as every other `data-*`-driven state in this codebase). Verified in a real browser (Playwright): default/checked/indeterminate/error/disabled/disabled+checked states, click-to-toggle, Tab focus ring, and a multi-item `Stack` layout all render correctly, no console errors.

No functionality gaps found relative to `Input`'s feature set when this module was reviewed for missing behavior — `checked`/`defaultChecked`/`indeterminate`/`onChange` already flow through `{...rest}` untouched, so no new tests were added at the `Checkbox` level beyond what `UnstyledCheckbox`'s own suite and this package's 14 label/error/id/ref/passthrough tests already cover.

**`@asnewyla/unstyled-radio`** — Done. Same shape as `UnstyledCheckbox` minus `indeterminate` (not a radio concept): `UnstyledRadioProps = { checked?, defaultChecked?, onChange?, disabled?, invalid? } & Omit<InputHTMLAttributes<HTMLInputElement>, 'type'|'checked'|'defaultChecked'|'onChange'|'disabled'|'invalid'>`. One real architectural difference from `Checkbox`, found via a first draft that copied `Checkbox`'s always-pass-`checked` approach directly: doing that for radio silently breaks native browser radio-group exclusivity. Passing `checked={isChecked}` unconditionally makes React treat the input as permanently controlled, so when a sibling radio sharing `name` gets clicked, React re-asserts *this* radio's own local state back onto the DOM on the next render — fighting the browser's own attempt to uncheck it. Fixed by only passing `checked` when the consumer is actually controlling it (`checked !== undefined`); otherwise `defaultChecked` is passed instead, so the DOM genuinely owns the checked state and native exclusivity works.

Accepted, documented limitation (its own dedicated test, not just a comment, per the established convention for this): even with that fix, an uncontrolled radio's own `data-checked` mirror can go stale when a *sibling* is checked instead — browsers never fire a `change` event on the radio that gets deselected as a side effect, so its internal tracking never learns about it, even though the underlying DOM `.checked` is correctly `false`. `UnstyledRadio` alone is reliable for a single isolated radio or fully-controlled usage; a real multi-radio group needs `RadioGroup` (planned next) as one shared source of truth instead.

Also caught during review of a first draft (worth remembering for future primitives copied from `UnstyledCheckbox`): (1) `UnstyledRadioProps` didn't extend native input attributes at all (a hand-rolled object type, not `Omit<InputHTMLAttributes, ...>`) — `tsc --noEmit` genuinely failed (`name`/`value` didn't exist on the type) while `vitest` passed anyway, since it doesn't fully type-check; same class of gap `Grid` had. (2) `data-disabled`/`data-invalid` used the raw boolean prop instead of `booleanToString()` — confirmed empirically that `data-x={false}` renders as a real `data-x="false"` attribute rather than being omitted, which would break a `[data-disabled]` presence-selector in CSS. (3) `{...rest}` spread last instead of first, the same ordering gotcha as `Grid`'s.

**`@asnewyla/radio`** — Done. `RadioProps = Omit<UnstyledRadioProps, 'invalid'> & { label: string; error?: string }` — same port-of-`Input`'s-shape pattern as `Checkbox`. `Radio.css` renders a circle (`border-radius: 50%`) with a centered filled-dot `::after` when `[data-checked="true"]`, rather than `Checkbox`'s checkmark — otherwise the same token usage and `data-*`-driven state convention.

Three real bugs found reviewing a first draft, none caught by the passing test suite or `tsc`: (1) `className={className ? \`xd-radio-input ${className}\` : className}` — the fallback branch returned `className` itself (`undefined`) instead of the base class, so with no consumer `className` the input's `className` was an empty string and `xd-radio-input` never applied at all. Only surfaced via a manual render check, since the existing test only ever exercises the case where a consumer *does* pass a `className`. (2) A separate `invalid` prop was re-added to `RadioProps` after being `Omit`'d from `UnstyledRadioProps` — directly against the "`invalid` derived from `!!error`, not a separate prop" convention `Checkbox` already follows, and actually dead code: `UnstyledRadio` spreads `{...rest}` first and computes its own `data-invalid`/`aria-invalid` from `invalid={!!error}` after, so the extra props Radio passed were silently discarded every render. (3) The outer wrapper `<div>` had no `className` at all, so `Radio.css`'s layout rules had nothing to target.

Verified live in Storybook (Playwright): all visual states correct, native browser radio-group exclusivity confirmed working at the DOM level for two `Radio`s sharing `name` (clicking one genuinely unchecks the other's `.checked` property). But the first version of the `InAStack` story used two *uncontrolled* `Radio`s sharing `name` — and visibly reproduced `unstyled-radio`'s documented limitation: after clicking "Medium", the screenshot showed both "Small" and "Medium" rendered as filled/checked, even though `.checked` on "Small" was confirmed `false` at the DOM level. `data-checked` (what the CSS actually paints from) had gone stale. Initially rewritten as a controlled example (`checked`/`onChange` driven by React state) as a workaround; once `RadioGroup` shipped (below), the story (renamed `InAGroup`) switched to that instead, which is the real fix rather than a per-story workaround.

`Radio` also gained `RadioGroupContext`/`RadioGroupProvider`/`useRadioGroupContext` (in `RadioGroupContext.tsx`), consumed directly inside `RadioInner` — same weight-class direct consumption as `Button`'s `useButtonGroupContext()`, not a heavier wrapper like `FormFieldInput`. Precedence matches `Button`'s exactly: `checked ?? (group ? group.value === value : undefined)`, `name ?? group?.name` — an explicit own prop always wins over the group. `handleChange` calls both the consumer's own `onChange` *and* `group.onChange(String(value))` when inside a group, so standalone usage is completely unaffected by the group-awareness being present.

**`@asnewyla/radio-group`** — Done. Mirrors the `@asnewyla/button`/`@asnewyla/button-group` split exactly, for the same reason: `RadioGroupContext` lives in the *base* package (`@asnewyla/radio`) so `Radio` can consume it directly without depending on `radio-group`, while `RadioGroup` — the actual stateful wrapper — lives in its own package so a consumer who only wants one standalone `Radio` isn't forced to pull in group machinery. `RadioGroupProps = { name: string; value?: string; defaultValue?: string; onChange?: (value: string) => void; children: ReactNode } & Omit<ComponentPropsWithoutRef<'div'>, 'role'|'onChange'|'defaultValue'>` — uncontrolled by default (matching every other component in this codebase), renders `<div role="radiogroup">`. Unlike `ButtonGroupContext` (no local state, just passes props straight through), `RadioGroup` genuinely owns state (the selected value), so its context value follows the `FormFieldContext` memoization rule instead: `handleChange` wrapped in `useCallback`, the context value object wrapped in `useMemo`, both keyed off the real dependencies — skipping this would thrash on every unrelated re-render exactly as documented for that Pattern.

This is the actual, verified fix for `unstyled-radio`'s documented sibling-desync limitation — confirmed by literally reproducing the earlier broken screenshot's exact scenario (`InAGroup` story, click "Medium" then "Large") and getting the correct result each time: exactly one radio ever renders filled, no stale `data-checked`, because `RadioGroup` is the single owner of "which value is selected" and every member `Radio` re-renders from the same context value rather than tracking its own independent state.

**`@asnewyla/unstyled-select`** — Done (as an unstyled primitive; styled `@asnewyla/select` not started). Scope confirmed with the user before starting: single **and** multi-select in one component, client-side filtering only (no async/remote search) — matches the "async validation is out of scope for v0.x" precedent already set for `Form`.

`SelectOption = { value: string; label: string; disabled?: boolean }`. `UnstyledSelectProps` is a **discriminated union on `multiple`** — `{ multiple?: false; value?: string; defaultValue?: string; onChange?: (value: string) => void }` vs `{ multiple: true; value?: string[]; defaultValue?: string[]; onChange?: (value: string[]) => void }` — not a loose `value?: string | string[]`, so passing an array `defaultValue` without `multiple` (or vice versa) is a compile error, not a runtime bug. Verified via a throwaway probe (`@ts-expect-error` on both wrong shapes plus a wrong-arity `onChange`, all three correctly failed to compile; both right shapes compiled clean), deleted after confirming.

Options render as native `<button type="button" role="option">`, not `<div>` — a first draft used `<div role="option" onClick>`, which `pnpm lint` correctly flagged (`a11y/useKeyWithClickEvents`, non-focusable interactive element) since a plain `div` isn't in the tab order; fixed structurally (native buttons are focusable and fire `onClick` for both Enter and Space for free) rather than suppressed, matching [[feedback_avoid_lint_suppression]].

The trigger itself was a real `<button role="combobox">` through search/filter, but got restructured to a `<div role="combobox" tabIndex={0}>` once the styled `@asnewyla/select` needed removable multi-select chips: a `<button>` can't contain other interactive elements (nested buttons are invalid HTML), and a chip's own remove button is exactly that. Enter/Space-opens-the-listbox, which came free from native `<button>` semantics, is now hand-rolled on the div's `onKeyDown`; a disabled trigger gets `tabIndex={-1}` instead of the native `disabled` attribute. Confirmed with the user before doing it (bigger, more correct fix vs. a lighter keyboard-only-removal alternative that would have kept the `<button>`), since it changes the primitive's DOM shape.

Internal logic is split across three sibling hooks in `src/components/`, none exported from the package (matching `@asnewyla/form`'s `useFieldRegistry` precedent — internal-only, sibling to the component file, not under a separate `hooks/` directory): `useSelectValue` (controlled/uncontrolled value state, normalizes both the single and multi branches to one `selectedValues: string[]`, exposes `selectOption`), `useSelectOpenState` (`open` state, `containerRef`, click-outside/Escape-to-close, `toggle`/`close`), and `useRovingFocus` (trigger, search-input, and option refs; `focusTrigger`/`focusSearchInput`; `handleListboxKeyDown` for Arrow/Home/End). The component itself is a thin orchestration layer with no local state of its own beyond the search query.

A real hook-composition problem surfaced while wiring "return focus to the trigger after Escape or after selecting": `useRovingFocus`'s original "focus first option on open" effect needed `open` (from `useSelectOpenState`), while `useSelectOpenState`'s new `onEscape` callback needed `focusTrigger` (from `useRovingFocus`) — a genuine circular dependency between the two hook calls in the same render. Resolved by moving that effect out of `useRovingFocus` entirely: `toggle()` now uses `flushSync` (same pattern already established in `Form.tsx` for "focus something that just appeared") and returns the new `open` value, so the component calls a focus action imperatively right after a synchronous `toggle()`, with no effect and no ordering conflict. `focusTrigger` is wrapped in `useCallback` (stable identity) specifically because `useSelectOpenState` lists it as a real effect dependency — `pnpm lint` flagged the missing dependency, and the fix was to make the reference stable rather than add it unstably or suppress the warning.

Keyboard support: `ArrowDown`/`ArrowUp` move real DOM focus among options (roving focus, not `aria-activedescendant`), skip disabled options, and clamp at the ends (no wrap — a deliberate, easily-reversed simplicity choice, flagged to the user rather than silently picked); `Home`/`End` jump to the first/last enabled option; `Enter`/`Space` selection is free via native button semantics, no extra handling needed.

Search/filter was in scope from the start (see the scope-confirmation note above), not deferred. A search `<input aria-label="Search options">` renders inside the popup, above the option list — the trigger stays a `<button role="combobox">` unchanged, it does not become an editable input. This was a deliberate course-correction: the WAI-ARIA combobox pattern conventionally puts `role="combobox"` directly on an editable `<input>`, which was the first instinct, but doing that would have discarded the just-finished real-focus roving keyboard nav and everything built on it; keeping the trigger a button and adding the input inside the popup reuses `focusTrigger`, `useSelectOpenState`, and native Enter-selects-via-button behavior with zero rework, and multi-select search comes free since the trigger's label-rendering logic is untouched. Filtering is a case-insensitive substring match on `option.label`, computed via `useMemo` in the component (a pure derivation, not stateful behavior, so it isn't its own hook). `useRovingFocus` gained `setSearchInputRef`/`focusSearchInput`; the old `focusInitialOption` was removed entirely since opening now always focuses the search input rather than an option, and `ArrowUp` falls back to `focusSearchInput()` when there's no previous enabled option instead of stopping dead at the first one.

One real bug surfaced and got fixed during this block: when a focused option gets filtered out mid-search, the browser reverts real focus to `<body>` on unmount, and since keyboard events only bubble upward from wherever focus actually is, `<body>` isn't a descendant of the listbox's `onKeyDown` handler — so keystrokes stopped reaching the component entirely ("stranded focus"). Root-caused and fixed by always redirecting focus to the search input or an existing option rather than letting the browser pick a fallback.

50 tests, all passing for real reasons — up from an initial 39 before search/filter; four of the original 39 (three `ArrowUp` cases, `Home`) had briefly passed *vacuously* before the Arrow/Home logic existed, caught and called out explicitly rather than left as false confidence, then made to fail and pass for the right reason once the real logic landed.

`renderValue?: (selectedOptions: SelectOption[], helpers: { removeOption }) => ReactNode` was added for the trigger restructure above — when provided it replaces the default `selectedLabels.join(', ') : placeholder` text entirely, receiving real `SelectOption` objects (not just values, so a consumer doesn't have to re-look-up labels) plus a `removeOption` helper. `removeOption` isn't new logic — it's literally `selectOption` (the same toggle that powers clicking an option in the listbox), reused under a clearer name for this context, since calling it on an already-selected option always deselects. An `invalid` prop (`data-invalid`/`aria-invalid`) was added at the same time, matching every other input-shaped primitive in this library — closing the one item from the README's "Not yet supported" list that was actually trivial to add rather than a real scope decision. 61 tests total, up from 50.

Release-ready as a primitive: `README.md`, `LICENSE`, changeset, dry-run tarball verified. No Storybook stories yet — matches `unstyled-checkbox`/`unstyled-radio` precedent exactly, where the unstyled primitive alone never got its own stories, only the styled sibling did once it existed (nothing to visually demo without CSS). Still explicitly not built, called out in the README's own "Not yet supported" section: native `FormData` form participation, and portal rendering (the listbox renders inline, so it can be visually clipped by an `overflow: hidden` or scrolling ancestor).

**`@asnewyla/select`** — Done. Styled wrapper mirroring `unstyled-input → input` / `unstyled-checkbox → checkbox`: `label` (required), `error` (doubles as the invalid flag — no separate `invalid` prop exposed, same convention as `Input`/`Checkbox`), CSS driven by the same `data-*` attributes the primitive already exposes.

The popup listbox, its options, and the search input are all rendered internally by `UnstyledSelect` with no className hook of their own — extending the primitive further to expose one felt like scope creep for what's ultimately a CSS problem. Solved without touching the primitive at all: since the listbox is always the trigger's next sibling in the DOM (`UnstyledSelect`'s own container div wraps `<div role="combobox">` then, conditionally, `<div role="listbox">`), plain CSS adjacent-sibling + role selectors reach every internal piece from the outside — `.xd-select-trigger + [role="listbox"]`, `...[role="listbox"] [role="option"]`, `...[role="listbox"] input` — with zero new primitive API surface.

Multi-select chips are the one genuinely new interactive piece: each chip renders a real `<button aria-label="Remove <label>">`, calling `e.stopPropagation()` before `removeOption` so the click doesn't also bubble to the trigger's own click-to-toggle handler. `label`'s accessible-name association is `aria-labelledby`, not `htmlFor` — the trigger isn't a labelable element (only input/button/select/textarea/output/meter/progress are), so `htmlFor` alone would silently fail to name it for assistive tech. `htmlFor` is kept anyway alongside `aria-labelledby`: it's inert on a non-labelable target but satisfies `pnpm lint`'s `a11y/noLabelWithoutControl` (which only checks for `htmlFor`/nesting, not whether the target is actually labelable), avoiding a lint-suppression comment for a rule that's right in spirit even where its check is incomplete — matching [[feedback_avoid_lint_suppression]].

20 tests. Release-ready: `README.md`, `LICENSE`, changeset, dry-run tarball verified. Storybook stories exist for both `Select` and `UnstyledSelect` — the latter breaks the "unstyled primitive alone never gets stories" precedent deliberately, since its keyboard/search/multi-select interaction and the `renderValue` contract specifically are different enough from Checkbox/Radio's binary toggle to be worth demoing raw.

A first pass at `Select.css` shipped with real bugs, caught from a user-supplied screenshot review plus an independent CSS rewrite the user brought in for comparison: it referenced five tokens (`--xd-color-surface`, `--xd-color-surface-hover`, `--xd-shadow-md`, `--xd-color-focus`, `--xd-color-text`) that didn't exist anywhere in `@asnewyla/tokens` yet, silently falling back to hardcoded light-mode literals — so the popover ignored the active theme entirely and rendered white in dark mode. The popup was also in normal document flow (`margin-top`), so opening it reflowed the page. Fixed by adding a surface/text/border-strong/focus/shadow tier to `tokens.css` (and, since the rewrite covered `theme-paper`/`theme-sand` but not `theme-lavender`, deriving lavender's own values by hand from the same method — see the `@asnewyla/tokens` entry below) and switching the popup to absolute positioning. Also fixed in the same pass: a selected option and a focused option both drew a solid fill, so a selected+focused row swallowed the focus indicator (selected is now colour + a check mark, fill reserved for hover/focus); the listbox had no max-height; interactive edges used `--xd-color-border` (~1.3:1, fine for a decorative rule, fails WCAG 1.4.11's 3:1 for a control boundary) instead of the new `--xd-color-border-strong`. `Select.tsx`'s `renderValue` is now supplied unconditionally, not just for `multiple`, wrapping the single-select value and the empty-multi-select placeholder in `span.xd-select-value` — previously a bare text node that couldn't be dimmed or truncated.

**`@asnewyla/unstyled-table`** — Done. Explicit top user priority alongside Select. Scope: sortable/filterable table primitive, plain rendering plus single-column sorting, client-side global-search filtering (not per-column), row selection, and pagination — all confirmed with the user at each stage rather than assumed up front, same incremental-scope-confirmation approach as `unstyled-select`.

`UnstyledTableProps<T>` composes `Omit<ComponentPropsWithoutRef<'table'>, 'children'>` like every other primitive. Internal logic split across four sibling hooks in `src/components/` (matching `unstyled-select`'s `useSelectValue`/`useSelectOpenState`/`useRovingFocus` precedent): `useTableFilter`, `useTableSort`, `useTableSelection`, `useTablePagination` — composed in that fixed order in `UnstyledTableInner` (filter → sort → select → paginate) since each stage's output data feeds the next. Row identity (`rowIdentity.ts`, `getRowIdentity<T>(row, getRowKey?)`) falls back to `JSON.stringify(row)` when no `getRowKey` is given, and is what both React's `key` and selection tracking use — selection survives a sort-triggered reorder because of this, with a dedicated regression test (`UnstyledTable-RowIdentity.spec.tsx`) proving DOM state stays attached to the correct row.

Sortable headers render `<th aria-sort><button>`; a non-sortable column is a plain `<th>` (early return). Filtering is table-level (one `<input aria-label="Search table">` above the `<table>`), not per-column — an early draft put `filterable`/`onFilter` per-column with the search input nested inside a `<button>` (invalid HTML), restructured once the table-level scope was confirmed. Selection adds a checkbox column: per-row plus a header "select all" with native `indeterminate` (imperative ref + effect, same pattern `SelectAllHeaderCell` needed since `indeterminate` has no JSX/HTML attribute reflection — see `unstyled-checkbox`'s own note on this). Pagination slices the final (filtered, then sorted) row set and renders First/Previous/Next/Last buttons around a `"Page X of Y"` indicator; `currentPage = Math.min(rawPage, totalPages)` is a plain derived value (not an effect) so it self-heals when filtering shrinks the row count out from under the current page, with no risk of a momentary invalid-page render. Every pagination button carries `data-pagination-action="first"|"previous"|"next"|"last"` — added specifically as a styling hook for the (then-upcoming) styled `Table`, following the same "give the styled sibling a `data-*` hook before it needs one" move `unstyled-table`'s checkboxes already got (`data-checked`/`data-indeterminate` added proactively before `@asnewyla/table` existed).

First/Last-page navigation and the `data-pagination-action` attribute were added in a later pass, after the initial module shipped and was released: RED tests written and confirmed failing (6 new tests, 10 existing still passing) first, then `goToFirstPage`/`goToLastPage` (both just the existing shared `goToPage` helper targeting `1`/`totalPages`) and the two new buttons.

`unstyled-table`'s own test suite (51 tests) was split by scope from a single file into six: `UnstyledTable.spec.tsx` (base rendering), `-Sorting`, `-Filtering`, `-Selection`, `-Pagination`, `-RowIdentity` — see the spec-file-splitting Pattern below, which this module's review pass promoted from a one-off into a standing convention (`.mentor/MENTOR_CODEX.md`) and then applied retroactively to `unstyled-select`/`select`/`radio` too.

Release-ready: `README.md`, `LICENSE`, changeset, dry-run tarball verified, Storybook stories (`Default`/`Sortable`/`Filterable`/`Selectable`/`Paginated`/`FullFeatured`/`CustomCellRendering`/`BulkDeleteSelectedRows` — the last demonstrating that `UnstyledTable` never mutates `data`, deletion is entirely the consumer's own state one level up). Published, live at `0.2.0`.

**`@asnewyla/table`** — Done. Styled sibling, scope confirmed with the user up front via two explicit choices: full feature set in one pass (not staggered sorting-then-filtering-then-...) and thin wrapper — `className` + CSS only, no new props (`TableProps<T> = UnstyledTableProps<T>`). The user's own first draft had 6 real bugs caught by `tsc`/`tsup` but invisible to `vitest` (which doesn't type-check): a hand-rolled closed prop type instead of `UnstyledTableProps<T>`, a missing generic-`forwardRef` cast (collapsed `T` to `unknown`), `id={id || 'table'}` defaulting every table without an explicit id to the literal `"table"` (guaranteed collisions with 2+ tables on a page), `React.ForwardedRef` missing its type argument, reversed className merge order, and no `Table.css` at all (broke the package's own `./styles.css` export contract) — all fixed in one refactor pass per the user's explicit invitation.

The one real architectural constraint discovered while writing `Table.css`: `UnstyledTable` renders the search input and the pagination `<div>` as *siblings* of `<table>`, not inside a shared wrapper, and `className` only reaches the `<table>` itself. The pagination div is still reachable (`table.xd-table + div`, an adjacent sibling that *follows*), but the search input is not — it renders *before* the table, and CSS has no "previous sibling" combinator. Accepted rather than fixed (fixing it would mean `UnstyledTable` wrapping everything in a container div, out of scope for a CSS-only pass): the search input is styled via a bare `input[aria-label="Search table"]` selector, unscoped to any one `Table` instance, documented in both the CSS file header and the README. Pagination buttons render as CSS-drawn icon chevrons (the same border-trick geometry as `Select`'s chevron, no icon asset or new prop) keyed off `data-pagination-action`; the button text stays in the DOM as the accessible name, hidden via `font-size: 0` rather than `display: none` so screen readers still get it. Footer alignment defaults to right (`justify-content: flex-end`) via `--xd-table-pagination-justify`, a CSS custom property overridable from any ancestor — not a component prop, since the "no new props" scope was a deliberate constraint, not an oversight to work around.

Hit `noDescendingSpecificity` warnings from `pnpm check` where the header's sort-button rule and the pagination's button rule both end in a bare `button` compound selector (biome's rule can't see that the two live in structurally disjoint DOM regions — one inside `<table>`, one in its sibling `<div>` — and so can never actually collide). Restructured what genuinely could be restructured (split one combined `@media (prefers-reduced-motion)` rule so a suppression comment could attach to the right branch at all), then used a one-line `biome-ignore` for the two that are real false positives — no clean structural fix exists short of CSS cascade layers, disproportionate for this.

One real wiring bug, not a `Table`/CSS bug: Storybook resolves workspace packages through their built `dist`, and `tsup` emits CSS as a separate file rather than injecting it into the JS bundle, so every styled package's stylesheet needs an explicit import in `.storybook/preview.tsx` — present for every other styled package but missed for `table` on first pass (rendered with zero styling in Storybook until caught from a user screenshot and added). Same root cause, unrelated bug found and fixed in the same pass: `UnstyledTable.stories.tsx`'s own demo styles and `unstyled-table`'s README both referenced `button[aria-label="Previous page"]`, a selector that never matched anything — those buttons only ever had text content, never an actual `aria-label`. Both replaced with `[data-pagination-action]` once that attribute existed.

Release-ready: `README.md`, `LICENSE`, changeset, dry-run tarball verified, Storybook stories mirroring `unstyled-table`'s own set. Published, live at `0.2.0`.

**`@asnewyla/unstyled-switch`** — Done. Same interactive-primitive shape as `unstyled-checkbox`, per the roadmap note, with two deliberate differences decided up front rather than copied blindly: no `indeterminate` (not a switch concept — always fully on or off), and a real `<input type="checkbox" role="switch">` rather than a hand-rolled `<div role="switch">` — the standard WAI-ARIA switch pattern, keeping native keyboard activation and form participation for free, with `role="switch"` as the only thing distinguishing it from a checkbox to assistive tech. `aria-checked` is set explicitly, always present as a `"true"`/`"false"` string (unlike `data-checked`, which is omitted when false) — a real browser computes the accessible checked state from `.checked` on its own even under the role override, but setting it explicitly is cheap insurance and is what makes the state testable in jsdom without full AT emulation.

Scaffolded and RED-tested first (module didn't exist at all yet — the test file's `import { UnstyledSwitch } from '../components'` genuinely failed to resolve, which is a valid RED state for a brand-new primitive, not something requiring a stub). The user's first implementation pass had four real bugs, all caught by running `vitest` *and* `tsc --noEmit` together — 17/19 tests passed at runtime while `tsc` had 18 compile errors, the same "vitest doesn't type-check" gap seen repeatedly elsewhere in this project: (1) `onChange`/`onFocus`/`onBlur` typed as required instead of optional, failing to compile almost every test that omits them; (2) `UnstyledSwitchProps` was a hand-rolled closed object type instead of extending native input attributes, so `name` didn't type-check either — same class of bug as `Grid`'s and `unstyled-radio`'s first drafts; (3) `handleChange` read `e.target.value` instead of `e.target.checked` — a checkbox's `.value` is a fixed string regardless of state, so `Boolean(e.target.value)` is always `true`, meaning the switch could be turned on but never off again; (4) `setInternalChecked` ran unconditionally instead of being guarded by `!isControlled`. What the first draft got right and didn't need fixing: `{...rest}` spread ordering, and forwarding `ref` directly with no internal `setRefs` — `Checkbox` only needs an internally-owned ref for `indeterminate`'s imperative DOM property, which `Switch` has no equivalent of, so the simpler direct forward is correct here rather than a shortcut.

No Storybook stories for the primitive alone — matches the `unstyled-checkbox`/`unstyled-radio` precedent (a binary toggle has nothing to demo without CSS); only `unstyled-select`/`unstyled-table` broke that precedent, deliberately, for genuinely complex interactions. Release-ready: `README.md`, `LICENSE`, changeset, dry-run tarball verified. Published, live at `0.2.0`.

**`@asnewyla/switch`** — Done. Styled sibling, mirrors `Checkbox.tsx` exactly: `SwitchProps = Omit<UnstyledSwitchProps, 'invalid'> & { label: string; error?: string }`, `useId()`-generated or explicit `id`, `error` doubling as the invalid flag via `aria-describedby`, `className` merged onto the input as `xd-switch-input` (base class first). `Switch.css` draws a real track/thumb toggle: the track is the actual checkbox input styled as a pill (`appearance: none`), the thumb is drawn from the track's own `::after` (no extra DOM element), sliding via `transform: translate()` on `[data-checked="true"]`. The slide distance (`1rem`) is a derived number, not a guess — documented inline as `track width (2.25rem) − thumb width (1rem) − 2×inset (2px each side) = 1rem`, the same "explain the derived number in a comment" convention `Card`'s `--_padding` note and `unstyled-table`'s pagination math already established, so the relationship between the three source numbers stays legible if any of them change later.

Real process incident during this module, worth remembering: this package's scaffold (config files + RED spec) got committed on the `unstyled-switch` branch, then the GREEN implementation was written directly (explicit "fix the build" request) but the branch got tangled — CI showed `packages/switch build: Cannot find src/index.ts` failing on a PR that was supposed to be scoped to `unstyled-switch` alone. Root cause: the styled `switch` package had been committed to that branch too, incomplete. Fixed by removing `packages/switch` from that branch entirely (`git rm -r --cached`) so the PR was cleanly scoped and could merge; the styled work came back stashed, to be recovered on its own branch. Unstashing onto a fresh branch (created *after* that removal had merged to `main`) surfaced a real git mechanic worth remembering: a `git stash` only captures the *working-tree diff* relative to whatever was committed at stash time — it does not re-capture files that were already correctly committed and untouched. Since the config files (`package.json`, `tsconfig.json`, `tsup.config.ts`, `vitest.config.ts`, `LICENSE`, `test-setup.ts`) were unmodified at stash time, they weren't in the stash's diff at all; once the commit they depended on was gone from history, popping the stash restored only the spec-file edits and the new component files, silently leaving the config files missing. Not a stash bug — recreating those files (identical to what had verified clean before) and rerunning the full `pnpm build`/`test`/`check` sequence was the correct fix, not a corrupted stash to debug.

Release-ready: `README.md`, `LICENSE`, changeset, dry-run tarball verified, Storybook stories mirroring `Checkbox.stories.tsx`'s set. `.storybook/preview.tsx`'s stylesheet import was added correctly on the first pass this time (the exact gap `table` had) and verified by grepping the built `storybook-static` output for `.xd-switch` before calling it done, not just trusting the build succeeded. Published, live at `0.2.0`.

**`@asnewyla/unstyled-textarea`** — Done. Same shape as `unstyled-input` — no internal state tracking at all (a native `<textarea>`'s `value`/`defaultValue` just flow straight through; text content is visually self-evident, same reasoning `Input` uses and `Checkbox`/`Switch` don't share, since *their* checked state needs a `data-*` mirror for a styled wrapper to hook into). One deliberate deviation from `Input`'s own precedent, decided before writing any code: `{...rest}` spreads *first* here, protected values after — `Input.tsx` spreads last, already flagged in this file's own Patterns as a latent bug class, left alone there since it already shipped, not worth repeating in a new primitive.

Scaffolded and RED-tested first, same as every primitive this session — module didn't exist yet, so the test file's import genuinely failed to resolve. The first implementation pass had the exact "vitest green, tsc red" gap seen repeatedly in this project (here, 15/15 runtime pass vs. 6 real `tsc` errors): `UnstyledTextareaProps` was a hand-rolled type with no native-attribute extension at all, so `defaultValue`/`value`/`rows`/`placeholder` didn't type-check despite every test passing at runtime. Root cause worth remembering generally: TypeScript types are erased before `vitest` runs, so a prop present in `{...rest}` at the JS level reaches the DOM correctly regardless of whether the *type* declares it — there is no possible behavioral test that could have caught this gap; only `tsc --noEmit` can, which is exactly why this project always runs both together, not `vitest` alone, as this exact scenario recurred for `Grid`, `unstyled-radio`, and `unstyled-switch`'s first drafts too.

Also fixed in the same pass, both genuinely behavioral (not type-only) and each backed by its own new regression test rather than just a comment: a redundant, explicit `role="textbox"` (a `<textarea>` already carries that role implicitly — `getByRole('textbox')` already matched it before this was ever added) removed; the `{...rest}` ordering fixed. The ordering fix was verified directly, not just asserted: reverted just the spread order, watched the new test fail exactly as predicted (a raw `data-focused="true"` prop silently overriding the internally-computed one), then restored the fix and confirmed green — proof the test genuinely drives the behavior it claims to, not just a passing assertion at the current state.

Release-ready: `README.md`, `LICENSE`, changeset, dry-run tarball verified. No Storybook stories for the primitive alone — same `unstyled-checkbox`/`unstyled-radio`/`unstyled-switch` precedent. Merged to `main`, not yet released.

**`@asnewyla/textarea`** — Done. Styled sibling, mirrors `Input.tsx` exactly (stacked label-above-field layout, not `Checkbox`/`Switch`'s inline-beside-field one — matches the shape of the thing being labeled): `TextareaProps = Omit<UnstyledTextareaProps, 'invalid'> & { label: string; error?: string }`, `useId()`-generated or explicit `id`, `error` doubling as the invalid flag via `aria-describedby`, `disabled` explicitly destructured and re-passed (redundant but matches the repeated `Input`/`Checkbox`/`Switch` convention exactly), `className` merged onto the field as `xd-textarea-input`.

First draft (written directly by the user, reviewed after) had one behavioral bug beyond the usual type gaps, caught by biome's linter as a real error, not just a style suggestion: `const generatedId = id ?? useId()` calls the `useId()` hook conditionally — skipped entirely whenever `id` is provided — a genuine Rules-of-Hooks violation (`lint/correctness/useHookAtTopLevel`), fixed by calling `useId()` unconditionally and merging the result after, matching every other styled component's exact pattern. Also had two bugs from bypassing the primitive's own computed state instead of using its public API: setting `aria-invalid` directly as a raw prop (silently overridden by `UnstyledTextarea`'s own internally-computed `aria-invalid`, which is set *after* `{...rest}` by design) instead of passing `invalid={!!error}`; and never setting `aria-describedby` at all. Both were caught by the RED test suite immediately (2 of 14 tests failing) and fixed with the minimal targeted change per the user's explicit request, deferring the full type/structure refactor (the `Omit`, the `useId()` fix, the CSS) to a separate pass afterward — a deliberate two-step split (fix only what the failing tests demand; refactor/build out the rest later) rather than doing everything in one motion.

`Textarea.css` mirrors `Input.css`'s token usage and states (border/focus-ring/invalid/disabled) with two textarea-specific differences: a taller `min-height` floor (`5.5rem` vs. `Input`'s single-line `--xd-control-height-md`) since a fixed height doesn't make sense for multi-line content, and `resize: vertical` instead of the browser's default `resize: both` — letting a stacked, block-level field grow wider than its own container breaks whatever layout it sits in, so only vertical growth is allowed.

Release-ready: `README.md`, `LICENSE`, changeset, dry-run tarball verified, Storybook stories mirroring `Checkbox.stories.tsx`'s set (`Default`/`WithValue`/`WithError`/`Disabled`/`CustomRows`/`InAStack`). `.storybook/preview.tsx` wiring and the built-output `.xd-textarea` grep-check both done correctly on the first pass. Merged to `main`, not yet released.

**`@asnewyla/unstyled-tabs`** — Done. Compound components (`Tabs`/`TabList`/`Tab`/`TabPanel`), scope confirmed with the user up front on the two decisions that actually shape the API: compound components over a data-driven `items` array (consumer controls markup directly, matches Radix/Reach's own shape), and automatic activation (arrow keys move focus *and* switch panels immediately — focus and "active" are the same state under this mode, which is why `Tab` needs no separate focus-tracking at all, just `activeValue === value`). Horizontal-only for v1 decided directly, not asked — same "wait for a second real need" precedent as `Card`'s variant prop and `Grid`'s alignment scope; a vertical `ArrowUp`/`ArrowDown` mode is a backward-compatible follow-up whenever it's actually needed.

`TabsContext` holds `{ baseId, activeValue, setActiveValue }` — `baseId` from a single `useId()` call in `Tabs` itself, which is what lets `Tab` and `TabPanel` independently derive matching `aria-controls`/`aria-labelledby`/`id` pairs from `(baseId, value)` alone, with no separate registration step between them. `useTabsValue` follows the `RadioGroupContext` memoization pattern (`setActiveValue` wrapped in `useCallback`, context value in `useMemo`) since `Tabs` genuinely owns state, not just passing props through. `TabList` doesn't track a list of tab refs anywhere — its keyboard handler queries its own DOM directly (`querySelectorAll('[role="tab"]:not(:disabled)')`) at keydown time, which is also what makes disabled-tab-skipping and wrap-around fall out for free from plain array index arithmetic, no dedicated disabled-handling branch needed. `TabPanel` returns `null` entirely when inactive, not just hidden via CSS — a deliberate choice, not an oversight, so inactive panel content (and any effects it would mount) genuinely doesn't exist in the tree.

13 of 19 first-pass test failures traced to one bug, and it was in the test file, not the implementation: `getPanel('Profile content')` assumed a `tabpanel`'s accessible name comes from its own body text, but ARIA computes it from `aria-labelledby` (correctly pointing at the tab, "Profile") — fixed by matching on the tab's name and checking body text separately via `toHaveTextContent`. One real primitive gap found afterward, while writing the styled sibling's focus CSS: `TabPanel` had no `tabIndex` at all, so keyboard users had no way to reach panel content when the panel contained no focusable child of its own — WAI-ARIA's own Tabs pattern calls for `tabIndex={0}` here. Fixed with a driving test in the primitive itself, not just the styled layer. Needed a `biome-ignore` for `lint/a11y/noNoninteractiveTabindex` — same justified exception category as `role="group"` on a `<div>` elsewhere in this repo, since the linter has no way to know a `role="tabpanel"` div is a real WAI-ARIA interactive landmark, not an arbitrary non-interactive one.

Release-ready: `README.md`, `LICENSE`, changeset, dry-run tarball verified. No Storybook stories for the primitive alone — same precedent as every other unstyled primitive without genuinely complex raw interaction to demo. Merged to `main`, not yet released.

**`@asnewyla/tabs`** — Done. Styled sibling, thin wrapper applied to all four compound pieces individually (`xd-tabs`/`xd-tab-list`/`xd-tab`/`xd-tab-panel`, each its own base class, `className` merged base-first) — no new props anywhere, matching `Table`'s "className + CSS only" convention rather than the label/error wrapper shape `Checkbox`/`Switch`/`Textarea` use, since Tabs has no single labeled field to wrap. The CSS-side-effect import lives only in `Tabs.tsx` (the piece a consumer always renders as the outer wrapper), not duplicated across the other three files.

`Tabs.css` draws a classic underline-indicator tab bar: a hairline border under the whole `TabList`, with the active tab's own 2px primary-colored bottom border overlapping it exactly at the seam via `margin-bottom: -1px` — a derived-relationship, not independent values, same "explain why these two numbers must move together" discipline as `Switch`'s slide-distance comment.

Built in three separately-committed stages: (1) component implementation with a bare CSS placeholder — 17/17 tests green on the first pass; (2) real CSS, which is also where the `TabPanel` `tabIndex` gap above was found and fixed in the primitive; (3) Storybook stories (`Default`/`WithDisabledTab`/`Controlled` — the last driving `value` from a plain button outside the tabs themselves, proving controlled usage isn't limited to clicking a tab), README, changesets for both packages, root `package.json`/`.storybook/preview.tsx` wiring, dry-run tarballs for both. `.xd-tabs` presence in the built `storybook-static` output verified by grep before calling it done, same discipline as every styled package since `table`'s missed-import incident.

Release-ready: `README.md`, `LICENSE`, changeset, dry-run tarball verified, Storybook stories. Merged to `main`, not yet released.

**`@asnewyla/unstyled-tooltip`** — Done. Compound (`Tooltip`/`TooltipTrigger`/`TooltipContent`), 30 tests. `Tooltip` root renders a `<span>` wrapper with inline `position: relative; display: inline-block` — a shrink-wrapped positioning context so the styled `TooltipContent` can place itself with plain CSS (`position: absolute` + `data-side`), no portal and no measurement. `TooltipContent` renders `<span role="tooltip">` (both root and content are `<span>` — a tooltip's content is phrasing content, and `<span>`-in-`<span>` stays valid, unlike a `<div>` wrapper). Only rendered while open (returns `null` otherwise, like `TabPanel`). `TooltipTrigger` takes a single child and `cloneElement`s it — `Children.only` (throws on 0/2+), then injects `aria-describedby` (only while open, merged with any the child already has), `onPointerEnter`/`Leave`/`Down`, `onFocus`/`onBlur`, each composed with the child's own via a local `composeEventHandlers` (both always run — a tooltip never lets a consumer silently suppress its open/close). Does **not** override the child's `ref` — `cloneElement` preserves it, so no ref-merge machinery is needed at this layer. `useTooltipOpen` is the boolean sibling of `useTabsValue` (controlled/uncontrolled `open`/`defaultOpen`/`onOpenChange`). `delay` (default 700ms) applies to hover only; keyboard focus opens with no delay (a separate `openNow` vs `openWithDelay` in the context). The open timer is a `useRef` cleared on every state transition and on unmount. Escape-to-close is a `document` `keydown` listener attached only while open (WAI-ARIA APG's one hard requirement), with a dedicated unmount-cleanup test. Not built for v0.x, each called out in the README's "Not yet supported": collision-aware positioning, portal rendering (same clip-by-`overflow` caveat as `unstyled-select`), and hoverable content (pointer onto the tooltip closes it).

**`@asnewyla/tooltip`** — Done. Styled sibling, thin wrapper — `className` + CSS only, no new props (`TooltipProps = UnstyledTooltipProps`), matching `Table`/`Tabs`. Wraps `Tooltip` (adds `xd-tooltip-root`) and `TooltipContent` (adds `xd-tooltip`), re-exports `TooltipTrigger` from the primitive untouched (it renders no DOM of its own). CSS-side-effect import lives only in `Tooltip.tsx` (the always-rendered wrapper), per the `Tabs` precedent. `Tooltip.css`: the bubble is `position: absolute` against the wrapper, placed per `data-side` with a `0.5rem` offset and a 45°-rotated-square arrow (`::after`, `background-color: inherit`); `pointer-events: none` since it isn't hoverable; colors invert against the page (`--xd-color-text` background / `--xd-color-surface` text) so it reads in both light and dark with token fallbacks. `.xd-tooltip-root` has no rule of its own (the primitive's inline styles already do the work) — kept only as a consumer hook, documented in the CSS header rather than left as an empty block. 8 tests (class merge, `data-side` passthrough, ref, open/close still works, `aria-describedby` wiring). Storybook stories (`Default`/`Sides`/`NoOpenDelay`/`OnAnIconButton`), `.xd-tooltip` rules verified in `storybook-static`.

---

## Release Readiness

All packages built so far have `LICENSE`, `README.md`, a verified dry-run tarball, and are published live on npm — see Live packages above — except `unstyled-textarea`/`textarea` and `unstyled-tabs`/`tabs`, all four merged to `main` and release-ready but not yet run through `changeset version` + `changeset publish`. Every styled package has Storybook stories; unstyled primitives only do where the interaction is complex enough to be worth demoing raw (`unstyled-select`, `unstyled-table`) — the rest (`unstyled-checkbox`, `unstyled-radio`, `unstyled-switch`, `unstyled-textarea`, `unstyled-tabs`) deliberately don't.

**Post-release audit** (all 11 packages, cross-package): reviewed for refactor candidates, missing tests, and native-element-instead-of-`@asnewyla`-component gaps in Storybook demos. Fixed: `Form`'s error summary had zero CSS despite being an advertised feature (added `FormErrorSummary.css`, extracted the component to its own file to match the `validation.ts`/`useFieldRegistry.ts` split, re-added the `./styles.css` export removed earlier when `form` genuinely had no CSS); `storybook/Form.stories.tsx` used a raw `<button type="submit">` in all four stories instead of `@asnewyla/button`'s `Button`; `Button`/`Input`'s `className`-merging behavior was untested; `Card.spec.tsx` never combined `image` with `radius` in one test. Confirmed not a gap: `UnstyledButton`'s `isFocusVisible` has no test — verified via a throwaway probe that jsdom's `:focus-visible` can't distinguish keyboard from mouse focus, matching the same limitation already documented for `unstyled-input`.

---

## Next

### Planned modules, by priority

**Done**: ~~Theme (multi-brand theming)~~ and ~~Light/dark mode~~ — both shipped together as `@asnewyla/theme`'s `ThemeProvider` (`theme`/`mode`, independent, optional) plus `@asnewyla/tokens`'s `theme-paper.css`/`theme-sand.css`/`theme-lavender.css`. See the `@asnewyla/theme` Packages entry above. Merged to `main`, release pending.

**Done**: ~~Checkbox / Radio~~ — `@asnewyla/unstyled-checkbox`, `@asnewyla/checkbox`, `@asnewyla/unstyled-radio`, `@asnewyla/radio`, and `@asnewyla/radio-group` all shipped. See their Packages entries above. Still open, not blocking: wiring Checkbox or Radio into `@asnewyla/form`'s `FormFieldInput` (currently no consumer for boolean/choice fields, only text-like `Input`) — pick up whenever a real Form-validation need for either comes up. Merged to `main`, release pending.

**Done**: ~~Select / Dropdown~~ — explicit top user priority; scope confirmed (single + multi-select in one component, client-side filtering only). Both `@asnewyla/unstyled-select` (single/multi-select, full ARIA listbox keyboard navigation, search/filter, `renderValue`, `invalid`) and the styled `@asnewyla/select` (label/error, removable multi-select chips) are done and release-ready. See their Packages entries above. Portal rendering and native form participation are deliberately still open — see `unstyled-select`'s README "Not yet supported" section. `Theme`'s portal-cascade decision (attribute on `document.documentElement`, not a wrapping element) is already in place for whenever portal rendering is picked up.

**Done**: ~~Table~~ — explicit top user priority alongside Select. Both `@asnewyla/unstyled-table` (sorting, filtering, selection, pagination with first/last-page navigation) and the styled `@asnewyla/table` (thin wrapper, className + CSS only) are done, released, and live at `0.2.0`. See their Packages entries above.

**Done**: ~~Switch~~ — same interactive-primitive shape as Checkbox, picked next for exactly that reason. Both `@asnewyla/unstyled-switch` (native checkbox under a `role="switch"` override, no `indeterminate`) and the styled `@asnewyla/switch` (track/thumb CSS) are done, released, and live at `0.2.0`. See their Packages entries above.

**Done**: ~~Textarea~~ — same primitive/styled split as Input. Both `@asnewyla/unstyled-textarea` (no internal state, `{...rest}` spread first not last) and the styled `@asnewyla/textarea` (stacked label, `resize: vertical`) are done. See their Packages entries above. Merged to `main`, release pending.

**Done**: ~~Tabs~~ — scope confirmed up front (compound components `Tabs`/`TabList`/`Tab`/`TabPanel`, automatic activation, horizontal-only for v1). Both `@asnewyla/unstyled-tabs` (roving-focus keyboard nav, disabled-tab support, focusable active panel) and the styled `@asnewyla/tabs` (underline active-tab indicator, no new props) are done. See their Packages entries above. Merged to `main`, release pending.

**Done**: ~~Video~~ — `@asnewyla/video`, a single non-interactive component wrapping the native `<video>` element (no primitive/styled split — same category as `Image`/`Card`, whose idioms it mirrors). Scope: native browser controls only (no custom play/scrub/volume UI for v0.x), `src?: string` only (no multi-`<source>` array). `fit` (`data-fit`→`object-fit`, default `cover`), `radius` (`data-radius`, token-backed, `overflow: hidden` to clip the poster to the rounded corners), `aspectRatio` (inline style), `fallback?: string` src-swap-on-error (composes a consumer `onError`, `useEffect` reset on `src` change, no loop — identical to `Image`); everything else (`controls`/`autoPlay`/`loop`/`muted`/`poster`/`preload`/`playsInline`, `<track>` children) passes straight through. `data-testid` defaults to `"video"` (`<video>` has no ARIA role or implicit accessible name, so that's the query of record — `test-utils` exports `getVideo()`). `{...restProps}` spread *first*, protected attributes after — the `unstyled-textarea` convention, not `Image`'s spread-last. 24 tests. Release-ready: `README.md`, `LICENSE`, changeset, dry-run tarball (17 files), Storybook stories (`Default`/`AspectRatioAndRadius`/`Fit`/`AutoplayBackground`/`BrokenWithFallback`/`WithCaptions`) + `.storybook/preview.tsx` import, `.xd-video` rules verified in `storybook-static`. Storybook stories reference a short public sample clip (`w3schools.com/html/mov_bbb.mp4`) rather than a data URI — a `<video>` has nothing meaningful to show without a decodable stream, Storybook renders in a browser with network, and the package itself ships no assets; a deliberate divergence from `Image.stories`' data-URI-only approach. Merged to `main`, release pending.

**Done**: ~~Tooltip~~ — `@asnewyla/unstyled-tooltip` (primitive, 30 tests) + styled `@asnewyla/tooltip` (8 tests), both released, live at `0.2.0`. Compound (`Tooltip`/`TooltipTrigger`/`TooltipContent`), CSS-only positioning per `side`. See the three `Tooltip` Architecture Decision rows above and the two Packages entries below.

**In progress**: **Dialog** — `@asnewyla/unstyled-dialog` (compound primitive on the native `<dialog>` element) + styled `@asnewyla/dialog` (centered modal, `position` variant). See the five `Dialog` Architecture Decision rows above. Scaffold + block-1 RED tests (`Dialog` root + `DialogTrigger`, 13 tests) are in place on branch `feature/dialog`. Planned blocks: (1) root state + context + `DialogTrigger`; (2) `DialogContent` — native `<dialog>` lifecycle (`showModal`/`close`, `close` event → `onOpenChange(false)`, overlay-click dismiss); (3) `DialogTitle`/`DialogDescription` + `aria-labelledby`/`aria-describedby`; (4) `DialogClose` + focus return to trigger. Then styled layer + readiness.

**Tier 1**

**Tier 2**

- **Dialog** — see In progress above.

**Tier 3**

- **Collapsible**
- **Alert / Badge** — small, low-risk, mostly presentational (same category as `Card`).
- **Spinner / Skeleton** — loading-state gap noticed during this roadmap review; not in the user's original list but a common design-system need once async data (Table, Select) exists.
- **NavBar** — reconsider as a Storybook recipe built from existing `Layout`/`Group` rather than a new component, unless a real nav-specific behavior (active-route state, mobile collapse) turns up.
- **Calendar, Carousel** — most complex/scope-heavy of the list; defer until there's a real consumer need.

Revisit the Open Questions below (tsdown migration, `changesets/action` in CI) alongside this roadmap, not instead of it.

---

## Patterns Established Here

Referenced by later phases; do not re-derive.

- **Render props on primitives with a children slot** — `{isHovered,isPressed,isFocused,isFocusVisible,isDisabled}`. Callbacks only allow side effects, not conditional rendering.
- **`isFocusVisible` vs `isFocused`** — ring shows only on keyboard focus.
- **`data-*` for state** — set with `|| undefined` so falsy states leave no attribute.
- **`OverridableProps<T, Own>`** — shared polymorphic prop type, lives in each package's `utils/types.ts`; only for packages that expose `as` polymorphism.
- **Compose via `Omit<XProps, 'k'> & { ownFields }`, not hand-duplicated props** — a blocklist of what's disallowed, not an allowlist of what's forwarded, so future upstream props reach the wrapper with no maintenance.
- **`prop ?? group?.x ?? default` precedence** — test this directly in the package that owns the logic, not only through the downstream consumer.
- **`role="group"` on a `div`, not `<fieldset>`** — `<fieldset>` carries unwanted default browser chrome for a toolbar; the a11y lint is a deliberate `biome-ignore`, not an oversight.
- **Untyped `StoryObj` for render-only compound-demo stories** — `StoryObj<typeof meta>` forces `args` even for stories that only use `render`; type those specific exports as plain `StoryObj`.
- **Changeset before merge** — any PR changing a published package's behavior runs `pnpm changeset` before merge. Release is separate: `pnpm version-packages` then `pnpm release`.
- **Leading-underscore custom properties (`--_bg`, `--_bg-hover`, `--_fg`, ...) are private/computed, never tokens** — a `--xd-*` token is a stored value; `--_bg-hover` is `color-mix()` derived fresh from `--_bg`. Don't tokenize these — overriding the source token already produces a correctly-shaded derived state.
- **Bare CSS `entry` in tsup for CSS-only packages** — `entry: ['src/tokens.css']` builds straight to `dist/tokens.css`, no dummy JS import needed. Only for packages with genuinely no JS to ship.
- **"Reset state on prop change" effects need a `biome-ignore` for `useExhaustiveDependencies`** — e.g. `useEffect(() => setHasErrored(false), [src])`. The linter doesn't read `src` in the callback body and treats the dependency as extraneous; the suggested "fix" would silently break the reset.
- **Track a boolean flag, not a copy of the prop, when a fallback needs to reset on prop change** — avoids "stuck on stale fallback" bugs from manually syncing a copied value.
- **`forwardRef(Inner) as <T,>(...) => ReactElement` only when `Inner` itself is generic** — i.e. only components with a polymorphic `as` prop (`UnstyledButton`, `Button`). Every non-polymorphic component uses plain `forwardRef(Inner)`; the two coexisting forms are correct, not inconsistent.
- **`*Props` composition: `type X = Omit<Base, 'k'> & { ownFields }`, not `interface X extends Omit<Base, 'k'>`** — always `ComponentPropsWithoutRef`, never bare `ComponentProps`, when deriving from a `forwardRef` component (bare `ComponentProps` retains `RefAttributes`, duplicating `ref`). The polymorphic pair (`UnstyledButton`/`Button`) is the deliberate exception, driven by the `as`-prop machinery.
- **Storybook demos use this library's own components, never a native element that already has an `@asnewyla` equivalent** — a demo button is `@asnewyla/button`'s `Button` (or `IconButton`), a demo text field is `Input`, a demo image is `Image`, layout is `Layout`/`Stack`/`Group`, and so on — not `<button>`/`<input>`/`<img>`/raw flex `<div>`. The stories are the showcase; a native element in one is a bug (it either predates the component or slipped through). A story reaching for a raw layout div is also a signal something's wrong with `Layout`'s API. Exceptions, each deliberate and narrow: (1) `Tokens.stories.tsx`, since `tokens` sits upstream of `layout`; (2) a story whose whole subject *is* the raw contract — e.g. `UnstyledSelect.stories.tsx`'s `renderValue` demo, which exists to show what the unstyled primitive gives you with zero styling, so its `<button>` stays native on purpose (with a comment saying so). Elements with no component equivalent yet (`<p>`, `<h2>`, `<a>`, `<code>`) are fine.
- **`{...restProps}` position matters, differently for values vs. event handlers** — for a plain value never meant to be overridden, spread `{...rest}` first, the protected value after. For an event handler the component owns internal behavior through, reordering alone can't work — destructure the handler explicitly and compose both inside one internal handler (`onFocus?.(e)` called from `handleFocus`). Every primitive with internal state driven by a native event needs this from the start.
- **`tsup`'s `dts` build type-checks every file under `src/`, not just what's reachable from the entry point** — an unimported scratch file with type errors breaks the whole package's build. Keep genuinely-scratch exploration outside `src/`.
- **Token units: `rem` for anything that should scale with text-size preference, `px` only for deliberate fixed-pixel exceptions** (`radius-full`'s pill trick value; `border-width`/`focus-ring` hairline accents). Document the `px` exception in a comment next to the token, or it reads as a forgotten conversion.
- **A context provider with local state must memoize both its callbacks (`useCallback`) and the value object (`useMemo`)** — otherwise a consumer whose effect depends on the whole object thrashes on every unrelated state change, wiping state written earlier in the same tick. The consumer's effect should also depend on the specific callbacks it needs, not the whole context object. Every context that manages its own state needs this from the start.
- **Split a context-consuming concern into a wrapper component once it stops being a one-line `??` fallback** — same underlying rule as direct consumption ("plain component, no-op gracefully without the ancestor context"), different structural choice based on the weight of what the context adds, not the shape of the problem.
- **A `<form>` doing its own JS validation needs `noValidate`, even though its fields keep real native `required`/`pattern`/etc. attributes** — those attributes still have to be real (native validation must keep working for a field used outside `Form`), but left enabled on the `<form>` itself, the browser's own constraint validation silently blocks the `submit` event before any JS handler runs on an invalid required field — no error thrown, no event at all. jsdom 24 replicates this. A test that only asserts "`onSubmit` wasn't called" can pass for the wrong reason (native validation blocked it, not the component's own logic) and hide this for a long time — the failed-submit-then-something-visible-happens tests (focus moving, a summary appearing) are what actually caught it here, because those need the handler to run at all.
- **Prefer `flushSync` over a `useEffect` with a narrowed dependency array for "focus/scroll to a thing that just appeared in response to this event"** — `Form`'s error summary first tried a `useEffect` keyed only on a submit-attempt counter (reading `invalid.length` in the body without listing it as a dependency, needing a `biome-ignore lint/correctness/useExhaustiveDependencies`). The actual fix: wrap the validation state updates in `flushSync` (`react-dom`) inside `handleSubmit` itself, so the DOM is already updated by the time `.focus()` is called right there — no effect, no dependency array, no suppression comment at all. When a lint rule like this fires, look for a restructure that makes it genuinely satisfied before reaching for a suppression; a comment justifying a narrowed dependency array is a sign the code might be forcing something imperative (an event-driven side effect) into something reactive (an effect), not a shape to preserve.
- **`npm view`/`npm show` right after a publish can falsely 404 even though the publish genuinely succeeded** — npm's read path (what `npm view`/the website/`npm install` see) lags behind the write path (what `npm publish` commits to) by anywhere from seconds to longer. Seen twice now: once with a single package (`icon-button`, Module 7) and once across six packages published together in one `changeset publish` run (this session) — `npm view` 404'd on all six even after a 20s wait, while `changeset publish`'s own "Successfully published" output was correct. The reliable confirmation isn't `npm view` returning data — it's retrying the actual `npm publish`/`pnpm publish` for the same version: `npm error You cannot publish over the previously published versions` proves the version is genuinely live, is harmless to trigger (rejected, not a duplicate publish), and doesn't require waiting on propagation. Don't treat a post-publish `npm view` 404 as a real failure without trying this first.
- **A shared private custom property (`--_padding`-style) lets a padded container and a full-bleed child agree on one number without duplicating it** — `Card`'s padding and its full-bleed image's negative-margin offset both read the same `--_padding` (set once per `data-padding` value), so the image always negates exactly what the container applies, no matter which size is active. Same underlying convention as `--_bg`/`--_bg-hover` (private/computed, never a token) applied to a new problem shape: "one child needs to visually escape a padded container by exactly the padding amount." `overflow: hidden` plus the container's own `border-radius` then clips the escaped child for free — no separate radius prop needed on the child at all.
- **Spec files split by scope once a package's tests outgrow one file** — `Component-Scope.spec.tsx` per behavioral scope (e.g. `UnstyledTable-Sorting.spec.tsx`, `UnstyledTable-Filtering.spec.tsx`, `UnstyledTable-Selection.spec.tsx`, `UnstyledTable-Pagination.spec.tsx`, `UnstyledTable-RowIdentity.spec.tsx`, base rendering staying in `UnstyledTable.spec.tsx`), sharing genuinely-duplicated fixture data (not one-off local data) via a sibling `fixtures.ts`/`fixtures.tsx`. Applied to `unstyled-table` (1 file → 6), `unstyled-select` (1 file → 5: core mechanics, multi-select, keyboard navigation, search/filter, renderValue), `select` (1 file → 2: core, multi-select chips), `radio` (1 file → 2: core, inside `RadioGroupProvider`) — all verified with matching pre/post test counts (`unstyled-select` 61, `select` 20, `radio` 21, `unstyled-table` 43) and a full `pnpm check`/`pnpm test` pass. Deliberately *not* applied to `theme/ThemeProvider.spec.tsx` (141 lines, 11 tests across 2 describes — too small for the split to earn its keep) or `tokens/themes.spec.ts` (a `describe.each`/`it.each`-driven CSS regression matrix, not scoped component behavior — doesn't fit the pattern). Two real TS2742 ("inferred type cannot be named") errors surfaced while doing this, both from exporting a value out of a shared module whose type depends on an unexported library type: `storybook/UnstyledTable.stories.tsx`'s `meta` (needed an explicit `Meta<...>` annotation instead of relying on `satisfies` alone) and `unstyled-select`'s new `fixtures.tsx`'s `renderSelect` (needed an explicit `RenderResult` return-type annotation) — the fix in both cases is a plain type annotation on the exported value, since inference alone can't produce a portably-nameable type once it crosses a module boundary.
- **className merging: `className ? \`xd-base ${className}\` : 'xd-base'`, never a bare template literal** — every component that accepts a consumer `className` (`Button`, `Input`, `Image`, `Card`, `Layout`, `Grid`) uses this exact ternary. A bare `` `xd-base ${className}` `` renders the literal string `"xd-base undefined"` whenever a consumer doesn't pass `className` — a real bug caught this way in four already-styled components at once (Button/Input/Image/Card had all been converted to the bare form in one pass). It slipped past every existing test because `toHaveClass('xd-base')` only checks the given class is *present*, not that the full attribute is clean — it doesn't fail on extra garbage classes. Reviewed and deliberately kept as an inline ternary rather than extracted into a shared helper function/package: same reasoning as the `OverridableProps` duplication decision (see Open Questions) — small, cheap-to-duplicate logic stays duplicated per file until proven painful, and `@asnewyla/tokens` specifically can't hold it since that package is CSS-only by design, no JS entry point at all. The older `['xd-base', className].filter(Boolean).join(' ')` form (still correct, just harder to read at a glance) was the convention before this; both avoid the bug, the ternary is now the standard going forward.

---

## Open Questions

- Is `tsdown` (tsup's actively-maintained successor) worth migrating to? Would likely fix CSS Modules support; not urgent.
- `unstyled-input` as a fully separate npm package doubles the package count for every future interactive primitive under the no-barrel rule. Not reconsidering yet — worth a harder look once a third interactive primitive shows up.
- Should `changesets/action` (automated "Version Packages" PR bot) be wired into CI, or is manual `pnpm changeset version` fine for a single-contributor repo? If yes: needs `permissions: contents: write, pull-requests: write`, an `NPM_TOKEN` secret, and removal of the manual `release` script in the same change.
