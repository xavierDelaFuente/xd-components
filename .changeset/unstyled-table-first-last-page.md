---
"@asnewyla/unstyled-table": minor
"@asnewyla/table": patch
---

Add "First page"/"Last page" pagination buttons alongside Previous/Next, and a `data-pagination-action` attribute on all four pagination buttons for styling hooks. `@asnewyla/table` now renders them as CSS-drawn icon chevrons instead of text.
