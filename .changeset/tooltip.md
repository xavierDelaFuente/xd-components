---
"@asnewyla/tooltip": minor
---

Add `@asnewyla/tooltip`, styled tooltip built on `@asnewyla/unstyled-tooltip` — a theme-aware bubble (inverts against the page) positioned per `side` with an 8px offset and arrow, no new props over the primitive.
